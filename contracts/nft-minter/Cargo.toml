[package]
name = "nft-minter"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = "23.1.0"

[dev-dependencies]
soroban-sdk = { version = "23.1.0", features = ["testutils"] }
